export enum URL_PATHS {
  SIGNUP = "/signup",
  DASHBOARD = "/dashboard",
  LOGIN = "/login",
}

export enum CONSTANT_VALUE {
  AUTH_TOKEN = "authToken",
}

export enum STATUS {
  IDLE = "IDLE",
  LOADING = "LOADING",
  FAILURE = "FAILURE",
  SUCCESS = "SUCCESS",
}

export enum SOCKET_EVENTS {
  CONNECT = "connect",
}
