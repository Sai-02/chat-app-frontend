export enum URL_PATHS {
  SIGNUP = "/signup",
}

export enum CONSTANT_VALUE {
  AUTH_TOKEN = "authToken",
}

export enum STATUS {
  IDLE = "IDLE",
  LOADING = "LOADING",
  FAILURE = "FAILURE",
  SUCCESS = "SUCCESS",
}
